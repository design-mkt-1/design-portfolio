---
import Base from '../../../layouts/Base.astro';
import MediaGallery from '../../../components/MediaGallery.astro';
import { url } from '../../../lib/site';
import { projects, getProject } from '../../../data/projects';

export function getStaticPaths() {
  return projects.filter((p) => p.videos.length > 0).map((p) => ({ params: { project: p.slug } }));
}

const { project } = Astro.params;
const p = getProject(project!)!;
---

<Base
  title={`${p.name} Videos — Marketing Solutions`}
  crumbs={[
    { label: 'Projects', href: '/' },
    { label: p.name, href: `/${p.slug}` },
    { label: 'Portfolio', href: `/${p.slug}/portfolio` },
    { label: 'Videos' },
  ]}
>
  <section class="container head">
    <a class="backlink reveal" href={url(`/${p.slug}/portfolio`)}>← Portfolio</a>
    <h1 class="reveal">{p.name} <span class="muted">Videos</span></h1>
    <p class="lede muted reveal">Each tile is the 1080×1080 cut. Open one to see every size.</p>
  </section>

  <section class="container">
    <MediaGallery items={p.videos} kind="video" />
  </section>
</Base>

<style>
  .head {
    padding-top: 40px;
    margin-bottom: 28px;
  }
  h1 {
    font-size: clamp(1.8rem, 4.5vw, 2.6rem);
    margin: 18px 0 6px;
  }
  .lede {
    font-size: 1.02rem;
  }
</style>
